#include "main.h"

/**
* _abs - compute the absolute value of the integer
* @n: integer number to be computed
* Return: absolute value of the int
*/
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	return (-n);
}
